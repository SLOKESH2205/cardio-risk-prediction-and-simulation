# Repository Audit

This audit was performed conservatively. Files used by the current pipeline, Streamlit app, tests, or saved artifacts are marked `Keep`. If usage was uncertain, the file or function was kept and noted instead of deleted.

| File or folder | Reason | Recommendation |
| --- | --- | --- |
| `app.py` | Compatibility launcher for `app/streamlit_app.py`. | Keep |
| `app/streamlit_app.py` | Main Streamlit UI and current application behavior. | Keep |
| `run_pipeline.py` | Main end-to-end pipeline entry point. | Keep |
| `predict_pipeline.py` | Sample prediction CLI for the saved model. | Keep |
| `config.py` | Central constants; not widely imported yet, but useful and harmless project configuration. | Keep |
| `setup.py` | Package metadata for editable installs/imports. | Keep |
| `runtime.txt` | Deployment/runtime metadata. | Keep |
| `heart_disease_prediction.py` | Legacy standalone experiment with separate training, interactive prompts, plotting, PDF generation, debug prints, and dependency on `fpdf`; not imported by the current project. | Delete |
| `src/ingest.py` | Current ingestion, cleaning, harmonization, and data-quality output logic. One debug `print` duplicated logger output. | Keep / clean dead print |
| `src/features.py` | Current feature engineering logic used by pipeline, app, tests, and prediction CLI. | Keep |
| `src/train.py` | Current model training, tuning, threshold analysis, artifact saving, and report generation. | Keep |
| `src/evaluate.py` | Current evaluation plots, threshold sweep, and model card generation. | Keep |
| `src/explainability.py` | SHAP and patient-facing explanation helpers used by app and pipeline. Some public helpers are not currently called but may be useful extension points. | Keep |
| `src/simulation.py` | What-if scenarios, uncertainty, and report text used by Streamlit. | Keep |
| `src/analysis.py` | Risk context, stability, drift, breakdown, and trajectory helpers used by Streamlit. | Keep |
| `src/segmentation.py` | Segmentation training, artifact compatibility, and app cluster prediction. | Keep |
| `src/utils.py` | Shared JSON/joblib/directory helpers. | Keep |
| `src/logger.py` | Shared logger factory. | Keep |
| `src/exception.py` | Custom ingestion exception used by `src/ingest.py`. | Keep |
| `src/services/reporting.py` | `ReportingService.save_population_summary()` is used by `run_pipeline.py`. | Keep |
| `tests/` | Current regression tests for features, training helpers, prediction columns, and simulation helpers. | Keep |
| `notebooks/01_EDA.ipynb` | Single exploratory notebook; no duplicate notebook found. | Keep |
| `data/raw/cardio_train.csv` | Raw Cardio dataset used by `DataIngestor`. | Keep |
| `data/raw/heart_disease.csv` | Raw Framingham dataset used by `DataIngestor`. | Keep |
| `data/cardio_train.csv` | Duplicate copy of `data/raw/cardio_train.csv`; current code reads from `data/raw/`. | Delete |
| `data/heart_disease.csv` | Duplicate copy of `data/raw/heart_disease.csv`; current code reads from `data/raw/`. | Delete |
| `data/processed/harmonized.csv` | Processed dataset used by Streamlit and generated by the pipeline. | Keep |
| `models/pipeline.pkl` | Current trained model used by app and prediction CLI. | Keep |
| `models/segmentation.pkl` | Current segmentation artifact used by app. | Keep |
| `outputs/baseline_best_model_metrics.json` | Baseline metrics used by `ModelTrainer._load_baseline_metrics()`. | Keep |
| `outputs/best_model_metrics.json` | Current selected-model metrics. | Keep |
| `outputs/best_hyperparameters.json` | Current tuning output generated by training. | Keep |
| `outputs/threshold_analysis.json` | Current threshold analysis output generated by training. | Keep |
| `outputs/threshold_sweep.csv` | Current evaluator threshold diagnostic output. | Keep |
| `outputs/feature_importance.csv` | Current feature importance output used by the model improvement report. | Keep |
| `outputs/model_improvement_report.md` | Current generated model improvement report. | Keep |
| `outputs/model_comparison.csv` | Used by the Streamlit Model Report tab. | Keep |
| `outputs/model_card.json` | Used by the Streamlit Model Report tab. | Keep |
| `outputs/data_quality_report.json` | Used by the Streamlit Model Report tab. | Keep |
| `outputs/data_quality_report.md` | Generated by current ingestion as a human-readable quality report. | Keep |
| `outputs/data_quality_report.html` | Generated by current ingestion as a human-readable quality report. | Keep |
| `outputs/risk_reference.csv` | Used by Streamlit percentile context. | Keep |
| `outputs/cluster_profiles.csv` | Used by Streamlit cluster comparison. | Keep |
| `outputs/cross_dataset_eval.csv` | Conditionally read by the Streamlit Model Report tab if present. | Keep |
| `outputs/cross_dataset_eval.json` | Duplicate legacy cross-dataset report; not generated or read by current code. | Delete |
| `outputs/roc_curve.png`, `outputs/pr_curve.png`, `outputs/confusion_matrix.png` | Used by the Streamlit Model Report tab. | Keep |
| `outputs/shap_global.png` | Used by the Streamlit Model Report tab. | Keep |
| `outputs/shap_global_bar.png` | Generated alongside the SHAP global plot; useful paired diagnostic. | Keep |
| `outputs/elbow_plot.png` | Generated by segmentation; useful diagnostic artifact. | Keep |
| `outputs/population_summary.json` | Generated by `ReportingService`; retained for reporting/dashboard support. | Keep |
| `outputs/debug_report.md` | Stale generated report not referenced by code. | Delete |
| `outputs/performance_report.md` | Stale generated report not referenced by code. | Delete |
| `.pytest_cache/`, `__pycache__/`, `.mypy_cache/`, `.ipynb_checkpoints/` | Generated caches; not source or required artifacts. | Delete |
| `.uv-cache/` | Local dependency cache; not source or required artifact. | Delete |
| `.venv/`, `.venv311/` | Local virtual environments. Removing them would disrupt local execution, so they were left untouched. | Keep |

## Dependency Notes

| Package | Usage | Recommendation |
| --- | --- | --- |
| `joblib` | Model/artifact serialization. | Keep |
| `matplotlib` | Evaluation, segmentation, and SHAP plots. | Keep |
| `numpy` | Training, evaluation, SHAP, and simulation. | Keep |
| `pandas` | Data processing, reporting, tests, and app tables. | Keep |
| `plotly` | Streamlit dashboard charts. | Keep |
| `pytest` | Test suite. | Keep |
| `scikit-learn` | Models, preprocessing, metrics, clustering. | Keep |
| `seaborn` | Confusion matrix plot. | Keep |
| `shap` | Explainability. | Keep |
| `streamlit` | App. | Keep |
| `xgboost` | XGBoost model. | Keep |

No unused package currently listed in `requirements.txt` was found after removing the legacy script.
