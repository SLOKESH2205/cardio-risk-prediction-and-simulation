# Repository Cleanup Report

## Files Removed

| Path | Reason |
| --- | --- |
| `heart_disease_prediction.py` | Obsolete standalone experiment not imported by the current app, pipeline, or tests. |
| `data/cardio_train.csv` | Duplicate raw dataset; `src/ingest.py` reads `data/raw/cardio_train.csv`. |
| `data/heart_disease.csv` | Duplicate raw dataset; `src/ingest.py` reads `data/raw/heart_disease.csv`. |
| `outputs/debug_report.md` | Stale generated report not referenced by code. |
| `outputs/performance_report.md` | Stale generated report not referenced by code. |
| `outputs/cross_dataset_eval.json` | Duplicate legacy output; the app only reads `outputs/cross_dataset_eval.csv`. |
| `.pytest_cache/`, `__pycache__/`, `.uv-cache/` | Generated cache folders. |

## Files Merged

No source files were merged. The current `src/` layout is already student-friendly and maps cleanly to project responsibilities: ingest, features, train, evaluate, explainability, segmentation, simulation, analysis, utilities, and reporting.

## Imports Removed

No import removals were required from active source files. Import ordering/spacing was normalized in:

- `app/streamlit_app.py`
- `src/services/reporting.py`

## Dead Code Removed

| Path | Change |
| --- | --- |
| `src/ingest.py` | Removed a debug `print()` that duplicated existing logger output and had no effect on data, model artifacts, metrics, or predictions. |
| `app/streamlit_app.py` | Removed trailing blank lines. |
| `src/segmentation.py` | Normalized excess blank lines around `describe_cluster()`. |

## Folder Changes

- Kept `data/raw/` as the single source of raw datasets.
- Kept `data/processed/` for generated harmonized data.
- Kept `outputs/` for artifacts read by the app or generated by the pipeline.
- Kept `.venv/` and `.venv311/` to avoid breaking local execution.
- Updated `.gitignore` so `.venv/`, `.venv*/`, `.pytest_cache/`, `.mypy_cache/`, and `.uv-cache/` stay out of future repository noise.

## Dependency Changes

No dependency was removed from `requirements.txt`. After deleting the legacy script, every listed package is still used by active code or tests.

## Verification Results

Baseline before cleanup:

- Best model: `XGBoost`
- CV AUC: `0.8055742705983817`
- Test AUC: `0.8056890814499387`
- F1: `0.7225647348951911`
- Sample prediction probability: `0.6970889568328857`

Post-cleanup verification:

- `.venv\Scripts\python.exe -m pytest`: passed, `8 passed`.
- `.venv\Scripts\python.exe predict_pipeline.py`: passed, sample output `{'risk_percent': 69.71}`.
- `.venv\Scripts\python.exe run_pipeline.py`: passed, pipeline completed and regenerated outputs.
- `.venv\Scripts\python.exe -m streamlit run app.py --server.headless true --server.port 8502`: passed startup smoke test; server stayed alive during the check and was stopped.

Post-cleanup metrics:

- Best model: `XGBoost`
- CV AUC: `0.8055742705983817`
- Test AUC: `0.8056890814499387`
- F1: `0.7225647348951911`
- Sample prediction probability: `0.6970889568328857`

Conclusion:

- Same metrics: yes.
- Same sample prediction: yes.
- Missing imports: none found during tests, prediction, pipeline, and Streamlit startup.
- Broken paths: none found during verification.

## Kept Due To Uncertainty

The following public helpers are currently lightly used or not directly called by the present UI, but were kept because they are plausible extension points and removing them could silently reduce functionality:

- `SHAPExplainer.generate_plain_text()`
- `SHAPExplainer.what_if_delta()`
- `SHAPExplainer.plot_waterfall()`
- `explain_scenario()`
- `generate_feature_impact_summary()`
- `config.py`
- `setup.py`
